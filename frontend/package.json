{
  "name": "frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "CHOKIDAR_USEPOLLING=1 nuxt dev --host 127.0.0.1 --port 4000 --no-public",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@nuxt/content": "^2.13.4",
    "@nuxtjs/tailwindcss": "^6.12.2",
    "@tailwindcss/typography": "^0.5.15",
    "marked": "^9.1.6",
    "nuxt": "^3.15.4"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
